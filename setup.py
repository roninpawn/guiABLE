from setuptools import setup, find_packages

setup(
    name='guiABLE',
    version='2.0.0',
    packages=find_packages(),
    install_requires=[],
    description='A skin-based GUI wrapper for tkinter',
    author='RoninPawn',
    include_package_data=True,
    python_requires='>=3.7',
)
